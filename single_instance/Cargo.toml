[package]
name = "single_instance"
version = "0.1.0"
edition = "2024"

[lib]
path = "./single_instance.rs"

[dependencies]
thiserror = { path = "../thiserror" }

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }
nix = { workspace = true }
