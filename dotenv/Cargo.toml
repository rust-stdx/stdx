[package]
name = "dotenv"
version = "0.1.0"
edition = "2024"
description = "Load environment variables from .env files"

[lib]
path = "./dotenv.rs"

[dependencies]
dotenv_derive = { path = "./dotenv_derive" }
memchr = { workspace = true, features = ["std"] }
