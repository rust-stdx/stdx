[package]
name = "crypt"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "crypt"
path = "crypt.rs"

[dependencies]
crypto = { path = "../../crypto" }
term = { path = "../../term" }
rand = { workspace = true }
zeroize = { workspace = true, features = ["alloc"] }
