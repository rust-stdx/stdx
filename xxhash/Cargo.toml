[package]
name = "xxhash"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "./xxhash.rs"

[dev-dependencies]
rand = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "hash"
harness = false
