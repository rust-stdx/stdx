[package]
name = "big_number"
version = "0.1.0"
edition = "2024"

[lib]
path = "./big_number.rs"

[features]
default = ["std"]
alloc = ["zeroize?/alloc"]
std = ["alloc"]
zeroize = ["dep:zeroize", "zeroize/derive"]

[dependencies]
zeroize = { workspace = true, optional = true }
