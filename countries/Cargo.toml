[package]
name = "countries"
version = "0.1.0"
edition = "2024"

[lib]
path = "./countries.rs"

[features]
default = ["std"]
std = ["serde/std"]
serde = ["serde/derive"]

[dependencies]
serde = { workspace = true, optional = true, default-features = false, features = ["derive", "alloc"] }
